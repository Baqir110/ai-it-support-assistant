# Evaluation Results

Generated by `scripts/evaluate.py` against the labeled set in that file (25 issues, including deliberately ambiguous cases). This scores the classifier and RAG retrieval, which are unaffected by whether an LLM is configured - the LLM only rewrites prose downstream of these decisions.

Re-run with `python scripts/evaluate.py` after any change to `app/services/classifier.py` or the knowledge base.

## Summary

| Metric | Value |
|---|---|
| Classification accuracy | 84.0% |
| Escalation-flag accuracy | 96.0% |
| Retrieval hit rate (correct doc in top-3) | 69.6% (23 applicable cases) |
| Avg latency | 6.66 ms |
| p95 latency | 2.52 ms |

## Per-category precision / recall / F1

| Category | Precision | Recall | F1 | Support |
|---|---|---|---|---|
| authentication | 0.8 | 0.8 | 0.8 | 5 |
| general | 0.667 | 1.0 | 0.8 | 2 |
| hardware | 1.0 | 0.833 | 0.909 | 6 |
| network | 0.6 | 0.75 | 0.667 | 4 |
| performance | 1.0 | 0.8 | 0.889 | 5 |
| system | 1.0 | 1.0 | 1.0 | 3 |

## Misclassified / notable cases

| Issue | Expected | Predicted | Retrieval hit |
|---|---|---|---|
| I can't reach any websites but Teams still works. | network | general | True |
| My internet connection drops every time it rains. | network | network | False |
| I can't sign in to my email, it says invalid credentials. | authentication | authentication | False |
| My computer has been extremely slow for the past two days. | performance | performance | False |
| The fan is running loud and everything is laggy. | performance | performance | False |
| The computer restarts randomly with no warning at all. | system | system | False |
| My monitor keeps losing signal whenever the network switch nearby cycles. | hardware | network | True |
| I think my account was compromised and now the VPN won't connect either. | authentication | network | False |
| Everything is slow and I also can't log in some mornings. | performance | authentication | False |

## Interpretation

- Misclassifications above are concentrated in the deliberately ambiguous cases (issues that plausibly belong to two categories), not the clear-cut ones - which is what you'd want a keyword-rule classifier's failure mode to look like.
- A retrieval miss means the top-k chunks didn't include the expected document even though the category was inferred correctly elsewhere in the pipeline; it's a signal for whether the knowledge base needs more coverage for that phrasing, independent of classification accuracy.
